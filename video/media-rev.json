{
  "rev": "v2",
  "_comment": "Bump on every media reshoot. Video URLs are cached immutably (max-age=1y) at the Tigris edge, so overwriting the same filename never propagates — the rev is baked into the uploaded filenames (render-all.mjs, render-videos.yml) and the site's requests (app/components/videoAssets.ts imports this file)."
}
